syntax = "proto3";

package proto;

message CovidData {
    string name = 1;
    string location = 2;
    string age = 3;
    string infected_type = 4;
    string state = 5;
}

service covidService {
    rpc handlerData(CovidData) returns (CovidData){};
}