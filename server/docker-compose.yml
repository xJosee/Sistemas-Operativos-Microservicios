version: "3.3"
services:
  node-api:
    build: .
    restart: always
    ports:
      - "4000:4000"
    links:
      - mongo-db
    volumes:
      - /proc /proc
  mongo-db:
    image: mongo
    restart: always
    ports:
      - "27017:27017"
